apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "txdecoder.fullname" . }}-test-connection"
  labels:
    {{- include "txdecoder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "txdecoder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
