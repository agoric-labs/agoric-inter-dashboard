apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oldapi.fullname" . }}-test-connection"
  labels:
    {{- include "oldapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oldapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
